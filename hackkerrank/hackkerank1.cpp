#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main() {
    int n, total = 0;
    cin >> n;
    vector<int> ar;
    for(int i = 0; i < n; i++) {
        int a = 0;
        cin >> a;
        ar.push_back(a);
    }
    sort()
}